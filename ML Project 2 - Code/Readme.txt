All the raw data sets are in the /data folder. And plots are in the /plots folder.

The whole /submit folder needs to be set as PYTHONPATH in system variables to work properly. Otherwise, the 3 modules in /utilities cannot be found.

There are 5 .py files in /src, which are the 3 python program for each part of the assignment.

Sampleouts are also generated and stored in /sampleout folder.